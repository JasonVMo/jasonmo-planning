# Tile view, version 1

Schema: `TileViewModel` — exactly `title`, `summary`, and entity-route `href`.
Useful for related entities and compact collections; permitted in every
render context. Render a descriptive accessible link and readable summary.

The same tile can represent different persisted data types through independent
validated adapters. No data-type dispatch or executable configuration.
